import React from 'react';
import {
    StyleSheet,
    View,
    Button
} from 'react-native';

const FirebaseComponent = ({ navigation }) => {
    return (
        <>
            <View >
                <Button title="Store Api Data" />
            </View>
        </>
    );
};

const styles = StyleSheet.create({
});

export default FirebaseComponent;
